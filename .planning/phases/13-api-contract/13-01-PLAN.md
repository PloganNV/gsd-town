---
phase: 13
plan: 01
name: api-contract
type: docs
autonomous: true
wave: 1
requirements: [DOCS-01, DOCS-02, DOCS-03]
---

# Phase 13 Plan 01: API Contract & Docs

## Objective

Publish the public API surface and versioning policy so contributors and downstream consumers have a stable contract to code against.

## Context

- `lib/gastown.sh` — 18 dispatch/polling/resilience functions (detection, dispatch, feedback, resilience)
- `lib/auto-setup.sh` — 3 lifecycle functions (detect_town, check_and_install_deps, bootstrap_town)
- `lib/bead-state.sh` — 4 bead-backed state functions (generate_state_from_beads, read_plan_result_from_bead, check_phase_completion_from_convoy, sync_requirements_from_beads)
- `lib/seance.sh` — 3 predecessor-context functions (get_seance_predecessors, build_seance_context, inject_seance_into_notes)

## Tasks

### Task 1 — Create CHANGELOG.md

Create `CHANGELOG.md` at repo root following [Keep-a-Changelog](https://keepachangelog.com/) format.

- `[Unreleased]` section (empty)
- `[0.1.0] — 2026-04-13` initial release section documenting major milestones

### Task 2 — Update README.md with Versioning and Public API sections

Add two new sections to README.md after the existing content:

- **## Versioning** — semver policy (pre/post 1.0)
- **## Public API** — scan-friendly tables per lib file listing public vs internal functions

## Verification

- `CHANGELOG.md` exists at repo root; `head -5` shows `# Changelog`
- `README.md` contains `## Versioning` and `## Public API` sections
- Public API tables show all functions from all 4 lib files

## Success Criteria

- DOCS-01, DOCS-02, DOCS-03 requirements marked complete
- No prose paragraphs where a table would serve — docs are scan-friendly

---
phase: 01-fork-and-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/laul_pogan/Source/gastown/.git/config
  - /Users/laul_pogan/Source/gastown/internal/daemon/convoy_manager_test.go
autonomous: true
requirements:
  - FORK-01
  - FORK-03
must_haves:
  truths:
    - "laulpogan/gastown fork exists on GitHub with upstream tracking configured"
    - "Local gastown clone has 'fork' remote pointing to laulpogan/gastown"
    - "Fix branch fix/3622-convoy-event-uuid is based on upstream merge base 677877bf (no gsd-town docs in diff)"
    - "A regression test in convoy_manager_test.go verifies GetAllEventsSince returns string IDs not int64"
    - "go test ./internal/daemon/ -run TestPollEvents_UUID passes"
  artifacts:
    - path: "/Users/laul_pogan/Source/gastown/internal/daemon/convoy_manager_test.go"
      provides: "UUID regression test — TestPollEvents_UUIDStringNotInt64"
      contains: "TestPollEvents_UUIDStringNotInt64"
  key_links:
    - from: "fix/3622-convoy-event-uuid branch"
      to: "upstream 677877bf merge base"
      via: "git checkout 677877bf && git checkout -b"
      pattern: "git log --oneline fix/3622..origin/main shows no gsd-town docs"
---

<objective>
Fork gastownhall/gastown to laulpogan/gastown on GitHub, configure upstream tracking on the local clone, create a clean fix branch for issue #3622, and add a regression test proving convoy event IDs are scanned as strings (not int64).

Purpose: FORK-01 establishes the GitHub fork structure required for all upstream PRs. FORK-03 addresses the convoy UUID scan bug — the current beads v1.0.0 source already has `string` in the right place, so the PR value is a regression test that would have caught any future regressions plus a clear comment explaining the CHAR(36) constraint.

Output: GitHub fork exists; local remotes configured; regression test committed to fix/3622-convoy-event-uuid branch, ready to PR upstream.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/laul_pogan/Source/gsd-town/.planning/PROJECT.md
@/Users/laul_pogan/Source/gsd-town/.planning/ROADMAP.md
@/Users/laul_pogan/Source/gsd-town/.planning/STATE.md
@/Users/laul_pogan/Source/gsd-town/.planning/phases/01-fork-and-critical-fixes/01-RESEARCH.md

<interfaces>
<!-- Key signatures the executor needs. Do not re-derive from source. -->

From /Users/laul_pogan/Source/gastown/internal/daemon/convoy_manager_test.go:
```go
// Existing test infrastructure — use setupTestStore for the new test
func setupTestStore(t *testing.T) (beadsdk.Storage, func()) {
    // Opens real beads DB via beadsdk.Open
    // Skips if Dolt unavailable (t.Skip)
}
```

From /Users/laul_pogan/go/pkg/mod/github.com/steveyegge/beads@v1.0.0/internal/types/types.go:
```go
// Event.ID is already typed as string — this is the verified fix
type Event struct {
    ID string // CHAR(36) UUID — must NOT be int64
    // ...
}
```

From /Users/laul_pogan/go/pkg/mod/github.com/steveyegge/beads@v1.0.0/internal/storage/issueops/events.go:
```go
// scanEvents scans rows.Scan(&event.ID, ...) where event.ID is string
// This is correct. The regression test guards against future type regressions.
func scanEvents(rows *sql.Rows) ([]*types.Event, error)
```

Fork commands (from RESEARCH.md):
```bash
gh repo fork gastownhall/gastown --clone=false
# Then in /Users/laul_pogan/Source/gastown:
git remote add fork https://github.com/laulpogan/gastown.git
git remote set-url --push origin https://github.com/laulpogan/gastown.git
# Verify
git remote -v
```

Fix branch creation (must be from upstream merge base, NOT local HEAD):
```bash
cd /Users/laul_pogan/Source/gastown
git fetch origin
git checkout 677877bf   # upstream merge base — no gsd-town docs here
git checkout -b fix/3622-convoy-event-uuid
# Verify no gsd-town docs bleed:
git diff origin/main..fix/3622-convoy-event-uuid -- "*.md" | head -5
# Should be empty or show only gastown docs, not .planning/ files
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub fork and configure remotes (FORK-01)</name>
  <files>/Users/laul_pogan/Source/gastown/.git/config</files>
  <read_first>
    Run: git -C /Users/laul_pogan/Source/gastown remote -v
    Run: gh auth status
    Run: gh repo list laulpogan --limit 20 | grep gastown
    These confirm current remote state and whether fork already exists.
  </read_first>
  <action>
    1. Create the fork (skip if laulpogan/gastown already exists):
       gh repo fork gastownhall/gastown --clone=false

    2. In /Users/laul_pogan/Source/gastown, add fork as a named remote:
       git remote add fork https://github.com/laulpogan/gastown.git

    3. Reconfigure push URL so `git push` goes to the fork by default:
       git remote set-url --push origin https://github.com/laulpogan/gastown.git

    4. Add the upstream read-only remote for future syncs:
       git remote add upstream https://github.com/gastownhall/gastown.git
       (skip if a remote named 'upstream' already exists)

    5. Fetch to populate remote-tracking refs:
       git fetch --all

    6. Verify:
       git remote -v
       Expected output shows: origin (fetch → gastownhall), origin (push → laulpogan/gastown), fork → laulpogan/gastown, upstream → gastownhall/gastown

    Note: The goal is minimal divergence from upstream. This remote config allows fetching upstream changes and pushing fixes to the fork for PR submission. Per FORK-01, no submodule in gsd-town — keep as separate repo.
  </action>
  <verify>
    <automated>
      cd /Users/laul_pogan/Source/gastown && git remote -v | grep laulpogan && gh repo view laulpogan/gastown --json name --jq '.name'
    </automated>
  </verify>
  <acceptance_criteria>
    - git remote -v shows origin push URL → https://github.com/laulpogan/gastown.git
    - git remote -v shows upstream fetch URL → https://github.com/gastownhall/gastown.git
    - gh repo view laulpogan/gastown returns successfully (fork exists on GitHub)
  </acceptance_criteria>
  <done>Fork exists on GitHub; local clone pushes to fork; upstream remote configured for future syncs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add UUID regression test on fix branch (FORK-03)</name>
  <files>/Users/laul_pogan/Source/gastown/internal/daemon/convoy_manager_test.go</files>
  <read_first>
    Read: /Users/laul_pogan/Source/gastown/internal/daemon/convoy_manager_test.go (full file — understand setupTestStore and existing test structure)
    Read: /Users/laul_pogan/Source/gastown/internal/daemon/convoy_manager.go lines 280-320 (GetAllEventsSince call site)
    Run: grep -n "setupTestStore\|GetAllEventsSince\|TestPoll" /Users/laul_pogan/Source/gastown/internal/daemon/convoy_manager_test.go | head -20
  </read_first>
  <behavior>
    - Test 1 (TestPollEvents_UUIDStringNotInt64): Insert a convoy event with a UUID string ID via the beads store, call GetAllEventsSince, verify returned event IDs are non-empty strings with length 36. A scan into int64 would panic/error — absence of error proves string scanning.
    - Test 2 edge case: If Dolt is unavailable, test skips gracefully (same as setupTestStore pattern — call t.Skip if store cannot be opened).
    - The test MUST fail if someone changes event.ID to int64 in the beads types — this is the regression guard.
  </behavior>
  <action>
    1. Create fix branch from upstream merge base (NOT local HEAD — avoids gsd-town docs bleeding into PR):
       cd /Users/laul_pogan/Source/gastown
       git fetch origin
       git checkout 677877bf
       git checkout -b fix/3622-convoy-event-uuid

    2. Verify branch is clean relative to upstream:
       git diff origin/main..fix/3622-convoy-event-uuid -- "*.md" | wc -l
       (should be 0 or only show gastown-owned .md files, not .planning/)

    3. Add TestPollEvents_UUIDStringNotInt64 to convoy_manager_test.go. Append to the file after existing tests. Use setupTestStore pattern. The test:
       - Calls setupTestStore(t) — skips if Dolt unavailable
       - Creates a test event via the store's Create/Insert method (inspect existing tests for the right call)
       - Calls store.GetAllEventsSince(ctx, time.Unix(0, 0).UTC())
       - Asserts err == nil
       - Asserts len(events) >= 1
       - For each event, asserts len(e.ID) == 36 (UUID string format)
       - Adds a comment: "// CHAR(36) UUID — scanning as int64 would produce 'converting []uint8 to int64' error (GH#3622)"

    4. Run the test (red first, then verify green):
       cd /Users/laul_pogan/Source/gastown && go test ./internal/daemon/ -run TestPollEvents_UUID -v -count=1
       If Dolt unavailable, test skips — that is acceptable (SKIP ≠ FAIL).
       If Dolt available, test must PASS (green).

    5. Commit:
       git add internal/daemon/convoy_manager_test.go
       git commit -m "test(daemon): regression test for convoy event UUID string scan (GH#3622)

    Adds TestPollEvents_UUIDStringNotInt64 to verify that GetAllEventsSince
    returns event IDs as strings (CHAR(36) UUID), not int64. The bug manifested
    as: 'converting driver.Value type []uint8 to int64: invalid syntax'.
    Current beads v1.0.0 scans event.ID as string — this test guards against
    future regressions.

    Closes #3622"

    6. Push to fork:
       git push fork fix/3622-convoy-event-uuid

    Note: Do NOT open the PR yet — that happens after the executor reviews the diff. The PR creation is a manual step (checkpoint handled by human when ready).
  </action>
  <verify>
    <automated>
      cd /Users/laul_pogan/Source/gastown && go test ./internal/daemon/ -run TestPollEvents_UUID -v -count=1 2>&1 | grep -E "PASS|SKIP|FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - fix/3622-convoy-event-uuid branch exists locally and on fork remote
    - git diff origin/main..fix/3622-convoy-event-uuid shows only convoy_manager_test.go changes (no .planning/ or gsd-town docs)
    - go test ./internal/daemon/ -run TestPollEvents_UUID -v returns PASS or SKIP (never FAIL)
    - Test function TestPollEvents_UUIDStringNotInt64 exists in convoy_manager_test.go
    - Test comment references GH#3622 and the int64 error message
  </acceptance_criteria>
  <done>UUID regression test committed to fix/3622-convoy-event-uuid and pushed to laulpogan/gastown fork. Branch is clean — diff against origin/main shows only the test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gh CLI → GitHub API | Fork creation uses authenticated gh CLI; credentials from system keychain |
| convoy_manager → beads store | SQL row scanning; type mismatch here causes runtime panic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | fix branch creation | mitigate | Branch from 677877bf (upstream merge base), verify diff shows no gsd-town docs before push |
| T-01-02 | Information Disclosure | gh repo fork | accept | Fork is public (gastownhall/gastown is public OSS); no private data involved |
| T-01-03 | Denial of Service | convoy event scan int64 | mitigate | Regression test in plan — scanning wrong type causes runtime panic in daemon; test catches before merge |
</threat_model>

<verification>
After both tasks complete:

1. Fork + remotes:
   cd /Users/laul_pogan/Source/gastown && git remote -v

2. Branch diff is clean (no gsd-town docs):
   git diff origin/main..fix/3622-convoy-event-uuid --name-only

3. Regression test passes or skips:
   go test ./internal/daemon/ -run TestPollEvents_UUID -v -count=1

4. GitHub fork accessible:
   gh repo view laulpogan/gastown
</verification>

<success_criteria>
- laulpogan/gastown fork exists on GitHub
- Local clone: origin pushes to fork, upstream remote tracks gastownhall/gastown
- fix/3622-convoy-event-uuid branch: pushed to fork, diff is clean (only convoy_manager_test.go)
- TestPollEvents_UUIDStringNotInt64 passes or skips (CHAR(36) UUID type verified)
- go build ./... still passes on this branch
</success_criteria>

<output>
After completion, create /Users/laul_pogan/Source/gsd-town/.planning/phases/01-fork-and-critical-fixes/01-01-SUMMARY.md
</output>
